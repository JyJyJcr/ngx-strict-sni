name: compile action test

on: push

jobs:
  compile:
    name: "compile test"
    runs-on: "ubuntu-latest"
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: cache
        uses: Swatinem/rust-cache@v2
      - name: Build binary
        run: cargo build
